// const { buildModule } = require("@nomicfoundation/hardhat-ignition/modules");

// export default buildModule("votingm", (m) => {
//   const voting = m.contract("VotingSystem");

// //   m.call(voting, "launch", []);

//   return { voting };
// });